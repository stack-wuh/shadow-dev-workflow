{
  "name": "shadow-dev-workflow",
  "version": "6.3.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test cli/test/*.test.mjs",
    "test:cli": "node --test cli/test/cli.test.mjs",
    "build:cli": "bun build --compile cli/src/index.mjs --outfile dist/shadow-dev.exe"
  },
  "bin": {
    "shadow-dev": "cli/src/index.mjs"
  }
}
