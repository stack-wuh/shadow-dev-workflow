{
  "name": "shadow-dev-cli",
  "version": "1.0.0",
  "description": "Deterministic workflow CLI of shadow-dev (single-file JSON protocol, plan/execute with planHash verification)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "shadow-dev": "src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "bun build --compile src/index.mjs --outfile dist/shadow-dev.exe"
  }
}
